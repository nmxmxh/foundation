\* Negative control: BrokenSpec injects a publish that decouples bucket from
\* status, so TearFreeRead MUST be violated. The formal-methods gate asserts
\* this model FAILS. See HermesProjectionPublishBroken.tla.
SPECIFICATION BrokenSpec

CONSTRAINT StateConstraint

INVARIANT TearFreeRead
