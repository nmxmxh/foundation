\* Negative control: BrokenSpec injects an enqueue that skips the write-queue
\* capacity guard, so WriteQueueBounded MUST be violated.
\* See MCWebSocketBackpressureBroken.tla.
SPECIFICATION BrokenSpec

CONSTANTS
  c1 = c1
  c2 = c2
  t1 = t1

CONSTRAINT StateConstraint

INVARIANT WriteQueueBounded
