\* Negative control: BrokenSpec injects a live enqueue that skips the MaxQueued
\* guard, so LiveQueueBounded MUST be violated.
\* See MCFrontendLiveProjectionBroken.tla.
SPECIFICATION BrokenSpec

CONSTANTS
  Tenant = Tenant
  OtherTenant = OtherTenant
  Domain = Domain
  Collection = Collection
  Record = Record
  NULL = NULL

CONSTRAINT StateConstraint

INVARIANT LiveQueueBounded
