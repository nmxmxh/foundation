\* Negative control: a non-idempotent counting fold, so StrongEventualConsistency
\* MUST be violated. See MCMetadataMergeBroken.tla.
SPECIFICATION Spec

CONSTANTS
  u1 = u1
  u2 = u2

CONSTRAINT StateConstraint

INVARIANT StrongEventualConsistency
