\* Negative control: BrokenSpec injects a refresh that stamps a version above the
\* watermark, so ProjectionVersionMonotonic MUST be violated.
\* See MCCacheProjectionFreshnessBroken.tla.
SPECIFICATION BrokenSpec

CONSTANTS
  k1 = k1
  v1 = v1
  v2 = v2

CONSTRAINT StateConstraint

INVARIANT ProjectionVersionMonotonic
