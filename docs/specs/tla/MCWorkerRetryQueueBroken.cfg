\* Negative control: BrokenSpec injects a retry that ignores the attempt budget,
\* so RetryBudgetBounded MUST be violated. See MCWorkerRetryQueueBroken.tla.
SPECIFICATION BrokenSpec

CONSTANTS
  j1 = j1
  j2 = j2

CONSTRAINT StateConstraint

INVARIANT RetryBudgetBounded
