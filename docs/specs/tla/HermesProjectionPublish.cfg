\* TLC config for the Hermes lock-free projection publish spec.
\* Run: FORMAL_RUN_TLC=1 TLA_TOOLS_JAR=/path/to/tla2tools.jar make check-formal-methods
\* or directly: java -cp tla2tools.jar tlc2.TLC -config HermesProjectionPublish.cfg HermesProjectionPublish.tla
SPECIFICATION Spec

CONSTANT MaxPublishes = 4

CONSTRAINT StateConstraint

INVARIANT TypeOK
INVARIANT TearFreeRead
INVARIANT VersionWatermarkConsistent
