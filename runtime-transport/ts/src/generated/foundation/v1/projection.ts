// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: foundation/v1/projection.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PayloadEncoding } from "./envelope";

export const protobufPackage = "foundation.v1";

export enum ProjectionOperation {
  PROJECTION_OPERATION_UNSPECIFIED = 0,
  PROJECTION_OPERATION_UPSERT = 1,
  PROJECTION_OPERATION_DELETE = 2,
  PROJECTION_OPERATION_PATCH = 3,
  UNRECOGNIZED = -1,
}

export interface ScalarValue {
  stringValue?: string | undefined;
  int64Value?: number | undefined;
  uint64Value?: number | undefined;
  doubleValue?: number | undefined;
  boolValue?: boolean | undefined;
  bytesValue?: Uint8Array | undefined;
}

export interface FieldValue {
  name: string;
  value?: ScalarValue | undefined;
}

export interface RecordMutation {
  operation: ProjectionOperation;
  sourceId: string;
  version: number;
  domain: string;
  collection: string;
  organizationId: string;
  recordId: string;
  fields: FieldValue[];
  vector: number[];
  createdAt?: Date | undefined;
  updatedAt?: Date | undefined;
  payload: Uint8Array;
  payloadEncoding: PayloadEncoding;
  payloadSchemaVersion: string;
  correlationId: string;
}

export interface RecordMutationBatch {
  mutations: RecordMutation[];
}

/**
 * ProjectionScope is the domain-agnostic key for the Hermes read path. It is
 * identical on both ends (server hermes.Store partitions and the frontend
 * HermesProjectionSource): a tenant (organization) projection partitioned by
 * domain and collection.
 */
export interface ProjectionScope {
  tenantId: string;
  domain: string;
  collection: string;
}

/**
 * ProjectionSnapshotRequest asks the gateway for the current materialized read
 * model at or after a resume watermark. It is the proto form of the frontend
 * ProjectionLoadRequest.
 */
export interface ProjectionSnapshotRequest {
  scope?:
    | ProjectionScope
    | undefined;
  /**
   * Optional resume token from a prior snapshot or delta. When set the gateway
   * returns only records whose version advanced past the watermark; an empty
   * value means from the beginning. Used for incremental catch-up (forward).
   */
  sinceWatermark: string;
  /** Optional cap on returned records. Zero means the gateway default. */
  limit: number;
  /**
   * Keyset pagination cursor (canonical foundation.v1 cursor convention; see
   * PageResponse.next_cursor). When set the gateway returns only records older
   * than this cursor, newest-first — the page after a prior response's
   * next_cursor. Used to backfill scopes larger than the limit without an
   * unbounded scan: each request stays O(limit). Empty means the first page.
   */
  cursor: string;
}

/**
 * ProjectionSnapshot is the materialized read model at a consistent epoch. The
 * records are carried as UPSERT RecordMutation entries so the snapshot and the
 * live delta stream share one wire shape (RecordMutationBatch). watermark/epoch
 * let the client resume the delta stream exactly where the snapshot ended.
 */
export interface ProjectionSnapshot {
  scope?: ProjectionScope | undefined;
  batch?:
    | RecordMutationBatch
    | undefined;
  /**
   * Monotonic resume token: the source watermark observed at snapshot time.
   * Present this as since_watermark to resume the live delta stream.
   */
  watermark: string;
  /** Hermes projection epoch the snapshot was read at. */
  epoch: number;
  /**
   * Keyset pagination cursor (canonical foundation.v1 next_cursor/has_more
   * convention): the cursor of the oldest record in this page. Present it as the
   * request cursor to fetch the next (older) page. Empty when no more pages.
   */
  nextCursor: string;
  /** True when more (older) records remain beyond this page. */
  hasMore: boolean;
}

/**
 * ProjectionSubscribeRequest opens a live delta stream for a scope, optionally
 * resuming after a watermark obtained from a snapshot or earlier delta.
 */
export interface ProjectionSubscribeRequest {
  scope?: ProjectionScope | undefined;
  resumeWatermark: string;
}

function createBaseScalarValue(): ScalarValue {
  return {
    stringValue: undefined,
    int64Value: undefined,
    uint64Value: undefined,
    doubleValue: undefined,
    boolValue: undefined,
    bytesValue: undefined,
  };
}

export const ScalarValue: MessageFns<ScalarValue> = {
  encode(message: ScalarValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringValue !== undefined) {
      writer.uint32(10).string(message.stringValue);
    }
    if (message.int64Value !== undefined) {
      writer.uint32(16).int64(message.int64Value);
    }
    if (message.uint64Value !== undefined) {
      writer.uint32(24).uint64(message.uint64Value);
    }
    if (message.doubleValue !== undefined) {
      writer.uint32(33).double(message.doubleValue);
    }
    if (message.boolValue !== undefined) {
      writer.uint32(40).bool(message.boolValue);
    }
    if (message.bytesValue !== undefined) {
      writer.uint32(50).bytes(message.bytesValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScalarValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScalarValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stringValue = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.int64Value = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uint64Value = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.doubleValue = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.boolValue = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bytesValue = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ScalarValue>, I>>(base?: I): ScalarValue {
    return ScalarValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScalarValue>, I>>(object: I): ScalarValue {
    const message = createBaseScalarValue();
    message.stringValue = object.stringValue ?? undefined;
    message.int64Value = object.int64Value ?? undefined;
    message.uint64Value = object.uint64Value ?? undefined;
    message.doubleValue = object.doubleValue ?? undefined;
    message.boolValue = object.boolValue ?? undefined;
    message.bytesValue = object.bytesValue ?? undefined;
    return message;
  },
};

function createBaseFieldValue(): FieldValue {
  return { name: "", value: undefined };
}

export const FieldValue: MessageFns<FieldValue> = {
  encode(message: FieldValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== undefined) {
      ScalarValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ScalarValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<FieldValue>, I>>(base?: I): FieldValue {
    return FieldValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldValue>, I>>(object: I): FieldValue {
    const message = createBaseFieldValue();
    message.name = object.name ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ScalarValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseRecordMutation(): RecordMutation {
  return {
    operation: 0,
    sourceId: "",
    version: 0,
    domain: "",
    collection: "",
    organizationId: "",
    recordId: "",
    fields: [],
    vector: [],
    createdAt: undefined,
    updatedAt: undefined,
    payload: new Uint8Array(0),
    payloadEncoding: 0,
    payloadSchemaVersion: "",
    correlationId: "",
  };
}

export const RecordMutation: MessageFns<RecordMutation> = {
  encode(message: RecordMutation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operation !== 0) {
      writer.uint32(8).int32(message.operation);
    }
    if (message.sourceId !== "") {
      writer.uint32(18).string(message.sourceId);
    }
    if (message.version !== 0) {
      writer.uint32(24).uint64(message.version);
    }
    if (message.domain !== "") {
      writer.uint32(34).string(message.domain);
    }
    if (message.collection !== "") {
      writer.uint32(42).string(message.collection);
    }
    if (message.organizationId !== "") {
      writer.uint32(50).string(message.organizationId);
    }
    if (message.recordId !== "") {
      writer.uint32(58).string(message.recordId);
    }
    for (const v of message.fields) {
      FieldValue.encode(v!, writer.uint32(66).fork()).join();
    }
    writer.uint32(74).fork();
    for (const v of message.vector) {
      writer.float(v);
    }
    writer.join();
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(90).fork()).join();
    }
    if (message.payload.length !== 0) {
      writer.uint32(98).bytes(message.payload);
    }
    if (message.payloadEncoding !== 0) {
      writer.uint32(104).int32(message.payloadEncoding);
    }
    if (message.payloadSchemaVersion !== "") {
      writer.uint32(114).string(message.payloadSchemaVersion);
    }
    if (message.correlationId !== "") {
      writer.uint32(122).string(message.correlationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordMutation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordMutation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.operation = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.version = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.collection = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.organizationId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.recordId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.fields.push(FieldValue.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag === 77) {
            message.vector.push(reader.float());

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vector.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.payloadEncoding = reader.int32() as any;
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payloadSchemaVersion = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RecordMutation>, I>>(base?: I): RecordMutation {
    return RecordMutation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordMutation>, I>>(object: I): RecordMutation {
    const message = createBaseRecordMutation();
    message.operation = object.operation ?? 0;
    message.sourceId = object.sourceId ?? "";
    message.version = object.version ?? 0;
    message.domain = object.domain ?? "";
    message.collection = object.collection ?? "";
    message.organizationId = object.organizationId ?? "";
    message.recordId = object.recordId ?? "";
    message.fields = object.fields?.map((e) => FieldValue.fromPartial(e)) || [];
    message.vector = object.vector?.map((e) => e) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.payload = object.payload ?? new Uint8Array(0);
    message.payloadEncoding = object.payloadEncoding ?? 0;
    message.payloadSchemaVersion = object.payloadSchemaVersion ?? "";
    message.correlationId = object.correlationId ?? "";
    return message;
  },
};

function createBaseRecordMutationBatch(): RecordMutationBatch {
  return { mutations: [] };
}

export const RecordMutationBatch: MessageFns<RecordMutationBatch> = {
  encode(message: RecordMutationBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.mutations) {
      RecordMutation.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordMutationBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordMutationBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mutations.push(RecordMutation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RecordMutationBatch>, I>>(base?: I): RecordMutationBatch {
    return RecordMutationBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordMutationBatch>, I>>(object: I): RecordMutationBatch {
    const message = createBaseRecordMutationBatch();
    message.mutations = object.mutations?.map((e) => RecordMutation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseProjectionScope(): ProjectionScope {
  return { tenantId: "", domain: "", collection: "" };
}

export const ProjectionScope: MessageFns<ProjectionScope> = {
  encode(message: ProjectionScope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.collection !== "") {
      writer.uint32(26).string(message.collection);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectionScope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectionScope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.collection = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProjectionScope>, I>>(base?: I): ProjectionScope {
    return ProjectionScope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectionScope>, I>>(object: I): ProjectionScope {
    const message = createBaseProjectionScope();
    message.tenantId = object.tenantId ?? "";
    message.domain = object.domain ?? "";
    message.collection = object.collection ?? "";
    return message;
  },
};

function createBaseProjectionSnapshotRequest(): ProjectionSnapshotRequest {
  return { scope: undefined, sinceWatermark: "", limit: 0, cursor: "" };
}

export const ProjectionSnapshotRequest: MessageFns<ProjectionSnapshotRequest> = {
  encode(message: ProjectionSnapshotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scope !== undefined) {
      ProjectionScope.encode(message.scope, writer.uint32(10).fork()).join();
    }
    if (message.sinceWatermark !== "") {
      writer.uint32(18).string(message.sinceWatermark);
    }
    if (message.limit !== 0) {
      writer.uint32(24).uint32(message.limit);
    }
    if (message.cursor !== "") {
      writer.uint32(34).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectionSnapshotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectionSnapshotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scope = ProjectionScope.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sinceWatermark = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProjectionSnapshotRequest>, I>>(base?: I): ProjectionSnapshotRequest {
    return ProjectionSnapshotRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectionSnapshotRequest>, I>>(object: I): ProjectionSnapshotRequest {
    const message = createBaseProjectionSnapshotRequest();
    message.scope = (object.scope !== undefined && object.scope !== null)
      ? ProjectionScope.fromPartial(object.scope)
      : undefined;
    message.sinceWatermark = object.sinceWatermark ?? "";
    message.limit = object.limit ?? 0;
    message.cursor = object.cursor ?? "";
    return message;
  },
};

function createBaseProjectionSnapshot(): ProjectionSnapshot {
  return { scope: undefined, batch: undefined, watermark: "", epoch: 0, nextCursor: "", hasMore: false };
}

export const ProjectionSnapshot: MessageFns<ProjectionSnapshot> = {
  encode(message: ProjectionSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scope !== undefined) {
      ProjectionScope.encode(message.scope, writer.uint32(10).fork()).join();
    }
    if (message.batch !== undefined) {
      RecordMutationBatch.encode(message.batch, writer.uint32(18).fork()).join();
    }
    if (message.watermark !== "") {
      writer.uint32(26).string(message.watermark);
    }
    if (message.epoch !== 0) {
      writer.uint32(32).uint64(message.epoch);
    }
    if (message.nextCursor !== "") {
      writer.uint32(42).string(message.nextCursor);
    }
    if (message.hasMore !== false) {
      writer.uint32(48).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectionSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectionSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scope = ProjectionScope.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.batch = RecordMutationBatch.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.watermark = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.epoch = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProjectionSnapshot>, I>>(base?: I): ProjectionSnapshot {
    return ProjectionSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectionSnapshot>, I>>(object: I): ProjectionSnapshot {
    const message = createBaseProjectionSnapshot();
    message.scope = (object.scope !== undefined && object.scope !== null)
      ? ProjectionScope.fromPartial(object.scope)
      : undefined;
    message.batch = (object.batch !== undefined && object.batch !== null)
      ? RecordMutationBatch.fromPartial(object.batch)
      : undefined;
    message.watermark = object.watermark ?? "";
    message.epoch = object.epoch ?? 0;
    message.nextCursor = object.nextCursor ?? "";
    message.hasMore = object.hasMore ?? false;
    return message;
  },
};

function createBaseProjectionSubscribeRequest(): ProjectionSubscribeRequest {
  return { scope: undefined, resumeWatermark: "" };
}

export const ProjectionSubscribeRequest: MessageFns<ProjectionSubscribeRequest> = {
  encode(message: ProjectionSubscribeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scope !== undefined) {
      ProjectionScope.encode(message.scope, writer.uint32(10).fork()).join();
    }
    if (message.resumeWatermark !== "") {
      writer.uint32(18).string(message.resumeWatermark);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectionSubscribeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectionSubscribeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scope = ProjectionScope.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resumeWatermark = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ProjectionSubscribeRequest>, I>>(base?: I): ProjectionSubscribeRequest {
    return ProjectionSubscribeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectionSubscribeRequest>, I>>(object: I): ProjectionSubscribeRequest {
    const message = createBaseProjectionSubscribeRequest();
    message.scope = (object.scope !== undefined && object.scope !== null)
      ? ProjectionScope.fromPartial(object.scope)
      : undefined;
    message.resumeWatermark = object.resumeWatermark ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
