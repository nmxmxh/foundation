// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: foundation/v1/envelope.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Metadata } from "./metadata";

export const protobufPackage = "foundation.v1";

export enum PayloadEncoding {
  PAYLOAD_ENCODING_UNSPECIFIED = 0,
  PAYLOAD_ENCODING_JSON = 1,
  PAYLOAD_ENCODING_PROTOBUF = 2,
  PAYLOAD_ENCODING_CAPNP = 3,
  PAYLOAD_ENCODING_BINARY = 4,
  UNRECOGNIZED = -1,
}

export interface EventEnvelope {
  id: string;
  eventType: string;
  payload: Uint8Array;
  metadata?: Metadata | undefined;
  correlationId: string;
  schemaVersion: string;
  occurredAt?: Date | undefined;
  payloadEncoding: PayloadEncoding;
  sourceNodeId: string;
}

export interface EventBatch {
  envelopes: EventEnvelope[];
}

function createBaseEventEnvelope(): EventEnvelope {
  return {
    id: "",
    eventType: "",
    payload: new Uint8Array(0),
    metadata: undefined,
    correlationId: "",
    schemaVersion: "",
    occurredAt: undefined,
    payloadEncoding: 0,
    sourceNodeId: "",
  };
}

export const EventEnvelope: MessageFns<EventEnvelope> = {
  encode(message: EventEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    if (message.correlationId !== "") {
      writer.uint32(42).string(message.correlationId);
    }
    if (message.schemaVersion !== "") {
      writer.uint32(50).string(message.schemaVersion);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(58).fork()).join();
    }
    if (message.payloadEncoding !== 0) {
      writer.uint32(64).int32(message.payloadEncoding);
    }
    if (message.sourceNodeId !== "") {
      writer.uint32(74).string(message.sourceNodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.schemaVersion = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.payloadEncoding = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.sourceNodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EventEnvelope>, I>>(base?: I): EventEnvelope {
    return EventEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventEnvelope>, I>>(object: I): EventEnvelope {
    const message = createBaseEventEnvelope();
    message.id = object.id ?? "";
    message.eventType = object.eventType ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.correlationId = object.correlationId ?? "";
    message.schemaVersion = object.schemaVersion ?? "";
    message.occurredAt = object.occurredAt ?? undefined;
    message.payloadEncoding = object.payloadEncoding ?? 0;
    message.sourceNodeId = object.sourceNodeId ?? "";
    return message;
  },
};

function createBaseEventBatch(): EventBatch {
  return { envelopes: [] };
}

export const EventBatch: MessageFns<EventBatch> = {
  encode(message: EventBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.envelopes) {
      EventEnvelope.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.envelopes.push(EventEnvelope.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<EventBatch>, I>>(base?: I): EventBatch {
    return EventBatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventBatch>, I>>(object: I): EventBatch {
    const message = createBaseEventBatch();
    message.envelopes = object.envelopes?.map((e) => EventEnvelope.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
