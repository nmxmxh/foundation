# syntax=docker/dockerfile:1.7

# Dockerfile for database migrations.
# Pin the migrate image for reproducible builds and global Docker image reuse.

ARG MIGRATE_VERSION=v4.17.0
FROM migrate/migrate:${MIGRATE_VERSION}

COPY migrations /migrations

WORKDIR /migrations

ENTRYPOINT ["migrate"]
