package main

import "testing"

func TestWorkerCommandPackageCompiles(t *testing.T) {
	// This smoke test keeps the worker command in the unit-test set while
	// allowing projects to customize worker helper signatures.
}
