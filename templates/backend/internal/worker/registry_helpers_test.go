package worker

import "testing"

func TestWorkerRegistryHelpersPackageCompiles(t *testing.T) {
	// This smoke test keeps worker registry helper code in the unit-test set
	// without assuming one queue configuration function signature.
}
