package worker

import "testing"

func TestWorkerRegistryPackageCompiles(t *testing.T) {
	// This smoke test keeps worker registry code in the unit-test set while
	// allowing project-specific worker dependencies and queue signatures.
}
