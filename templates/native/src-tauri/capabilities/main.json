{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Foundation native shell commands for the main app window.",
  "windows": ["main"],
  "permissions": [
    "allow-foundation-runtime-dispatch",
    "allow-foundation-runtime-capabilities"
  ]
}
