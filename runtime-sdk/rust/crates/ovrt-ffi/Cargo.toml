[package]
name = "ovrt-ffi"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
ovrt-native = { path = "../ovrt-native" }
ovrt-core = { path = "../ovrt-core" }
ovrt-unit = { path = "../ovrt-unit" }
